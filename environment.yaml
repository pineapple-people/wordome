name: wordome_env
channels:
  - conda-forge
dependencies:
  - beautifulsoup4
  - ruff
  - python=3.12.9
  - pip
  - pip:
    - curl_cffi # not available in conda-forge channel
    - -e .  # installs project (and its TOML deps) into the conda env
