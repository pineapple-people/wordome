[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wordome"
version = "0.1.0"
description = "A word frequency analysis tool"
dependencies = [
    "aiohttp",
    "beautifulsoup4",
]

[tool.setuptools.packages.find]
where = ["src"]